# VoiceGauntlet Agent Instructions

Follow the `.kiro/steering` guidance. Keep secrets out of tracked files. Prefer changes in `packages/core` for shared behavior and keep the demo UI wired to real data shapes.
