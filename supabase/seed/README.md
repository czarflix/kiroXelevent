# VoiceGauntlet Seed Data

The public demo is seeded in application code so the seeded scenario can be inspected without an account.

For live database testing, run:

```bash
npx -y supabase@2.93.0 db push
```

Then create a user through Supabase Auth and use `/app` to create real project records.
